import React from 'react';
import ReactDOM from 'react-dom/client';
import RetroBoard from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RetroBoard />
  </React.StrictMode>,
);
